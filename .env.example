# PostgreSQL connection string for the exports database.
# Used by the application to stream data. Format:
# postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://user:password@db:5432/exports_db

# Port on which the application listens (default: 8080).
PORT=8080
