# Tests

This folder is reserved for unit and integration tests.

Due to time constraints, automated tests were not implemented for this prototype. 
However, all API endpoints were manually validated using Postman and curl.